{
  "name": "@agentropolis/parallax-spatial-mcp",
  "version": "0.1.0",
  "description": "Governed closed-loop spatial operations for browser worlds.",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "quality": "npm run typecheck && npm test && npm run build"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
